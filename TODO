remove transformer call stacks?
can slice be read in before pass-off to arch-general display funcs?
complex and float type missing... but who cares for now? float probably easier to implement...
prevent crashy vma copy by halting memcpy() at brk() limit?
* remove worthless libelf dependency

Special jmpbuf problems resolved:
	jmp instruction
	immediate ret
jmpbuf problems remaining:
	call instruction
	rip-relative addressing
	modification of rsp
	*unknown* - require explicit exclusions
