* fix parsing of primitives < 64 bits wide on stack
remove transformer call stacks
support lookups on itabs
* analysis of clone call should not occur in dump_wait_state() function
can slice be read in before pass-off to arch-general display funcs?
getenv() is now broken in injected module code
* get libgomod.so from same dir as exe; make name automatic from build
* forced decoding on function returns doesn't seem to be working anymore
* doesn't return a value: func strings.IndexByte(s string, c byte) int
* runtime.gostring() creates error
* force re-launch on address space mapping conflict
* disappearing functions with the addition of tgkill()
